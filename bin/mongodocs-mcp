#!/usr/bin/env node

// Check for help flag first
if (process.argv.includes('--help') || process.argv.includes('-h')) {
  console.log(`
🚀 MongoDB Semantic MCP Server v9.0.0 - CLEAN ARCHITECTURE

USAGE:
  mongodocs-mcp                 Start the MCP server
  mongodocs-mcp --help         Show this help

CONFIGURATION:
  Model: voyage-context-3 (1024 dimensions)
  Search: MongoDB Atlas Vector Search + $rankFusion

ENVIRONMENT VARIABLES:
  MONGODB_URI              MongoDB Atlas connection string
  VOYAGE_API_KEY          Voyage AI API key

For more info: https://github.com/romiluz/mongodocs-mcp
`);
  process.exit(0);
}

// Import and run
import('../dist/index.js').catch(error => {
  console.error('❌ Failed to start MCP server:', error.message);
  process.exit(1);
});